const client = require('./client');
const React = require('react');
const Link = require('react-router').Link;

class PlayerDetails extends React.Component {
    render() {
        return (
            <div>Player Details here</div>
        );
    }

}

export {PlayerDetails};